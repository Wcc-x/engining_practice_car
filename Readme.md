# 本工程基于网上开源解算+esp官方工程库+自己统一接口编写而成
## 个人觉的ai写的太傻了，不如开源改一改,然后让ai给出注释，我自己根据注释写接口（虽然感觉开源写的不带脑子）
## 你可以见到很多注释，但其实代码都是开源的人写的，我为了接口方便，所以让ai加的注释，自己也就增增改改了几百行就结束了
### 补充：草，nnd，这开源写的有很大问题，我将构建pcnt读取，ledc驱动简单直流减速电机的简单pid工程。

# 强调：开发环境基于vscode+esp32idf 6.01(一定要注意版本区别)

## 根据解算，长宽的修改在ax_kinematics.c的186行，不懂的见我的解算照片

### 蓝牙模块我太懒了，底层蓝牙配置给ai了，适用于江协科技蓝牙串口
## deepseek写的一开始无法广播，有无法接收包，所以我用claude改写了一版，所以我只能用点好模型(claude还是太权威了)

### robot/ax_speed部分主要是四个电机的pid调用环节，写的很一般，但确实方便，只要给定两个参数就可以了
### Robot/ax_robot是给电机留下接口,个人感觉写的没什么用
### Robot/ax_Kinematic为四个电机的麦克轮子解算，186行double R  = MEC_WHEEL_BASE / 2.0 + MEC_ACLE_BASE / 2.0;   /* 0.153 m */，是个性化适配需要修改的参数

## 之前那一版本写的太大便了，我重构了
# 新打开工程激活插件可能CMakeLists有报错，这是需要写死编译器mingw的绝对路径！！！，这一点非常重要，不然编译无法完成

## 注意：有的ESP32特点是芯片自动复位，所以我的sdkconfig.defaults强行锁死芯片复位，降低flash SPI频率，波特率从408000改为115200（防止总线不稳定）【感谢deepseek】
## 在调试的时候要习惯串口打印，esp32自带uart转虚拟USB，但是不能占用tx rx引脚，打印串口能精确定义问题，在不同的函数后面加上打印，能够判断程序卡死在哪一步，加上build的日志，能够掌握报错信息，从而排查

# 讲一下数据流：蓝牙触发回调->拿到目标Vx,Vy->通过绑定示例的函数给到RI->解算到真实的Va...->数据流给到TI->PID计算->给到pwm->触发PWM的ax_hal速度解算->结合驱动板给到duty，输出PWM，同时pcnt一直在读取编码器计数
# define  MEC_WHEEL_SCALE     (PI * MEC_WHEEL_DIAMETER * PID_RATE / MEC_WHEEL_RESOLUTION)
## *   
 *  初始化 — 双通道 4x 正交解码
 *
 *  通道A: edge=MA, level=MB
 *          A↓→-1  A↑→+1 （B=HIGH保持 / B=LOW翻转）
 *  通道B: edge=MB, level=MA  （与A互补，构成4x解码）
 *          B↓→+1  B↑→-1 （A=HIGH保持 / A=LOW翻转）
 *
 *  正转 (A超前B) 每AB周期: A↓(+1) + B↑(+1) + A↑(+1) + B↓(+1) = +4
 *  反转 (B超前A) 每AB周期: A↓(-1) + B↑(-1) + A↑(-1) + B↓(-1) = -4
 *
 *  accum_count=true → 驱动内部自动累加溢出，无需 ISR
 *   
 ### ax_hal.c 85行,结合  把百分比占空比换算为硬件计数值，公式：计数值 = 占空比百分比 × 2^分辨率 / 100，得出duty = pwm_abs * 100 / AX_MOTOR_PWM_MAX